# 今天的宇宙小紙條

一個輕鬆、明亮風格的三張塔羅抽卡網頁。

## GitHub Pages

這是一個純靜態網站，可以直接部署到 GitHub Pages。

部署方式：

1. 將此資料夾推到 GitHub repository。
2. 到 repository 的 `Settings` -> `Pages`。
3. Source 選 `Deploy from a branch`。
4. Branch 選 `main`，資料夾選 `/root`。
5. 儲存後等待 GitHub Pages 產生網址。
